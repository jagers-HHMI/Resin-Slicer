{
  "name": "resin-slicer-electron",
  "version": "0.1.0",
  "private": true,
  "description": "Electron front end for the Python resin slicer backend.",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently -k \"vite --host 127.0.0.1\" \"wait-on tcp:5178 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5178 electron .\"",
    "build": "vite build",
    "start": "electron .",
    "postinstall": "node tools/ensure-electron.js",
    "check": "node --check electron/main.js && node --check electron/preload.js && node --check electron/renderer.js && node --check tools/ensure-electron.js && vite build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "electron": "^33.3.0",
    "vite": "^8.0.14",
    "wait-on": "^9.0.10"
  }
}
